# Read by docker-compose.yml itself (NOT by Laravel — that's backend/.env,
# a separate file). Copy to `.env` at the project root and fill in real
# values before running `docker-compose up`. Never commit the real `.env`.

DB_PASSWORD=
MYSQL_ROOT_PASSWORD=
