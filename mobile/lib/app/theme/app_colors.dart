import 'package:flutter/material.dart';

/// UI_UX_SPECIFICATION.md Part B.1/B.2 — canonical token set.
class AppColors {
  AppColors._();

  static const primary = Color(0xFF0D47A1);
  static const primaryContainer = Color(0xFF1565C0);
  static const secondary = Color(0xFF42A5F5);
  static const success = Color(0xFF2E7D32);
  static const warning = Color(0xFFF9A825);
  static const danger = Color(0xFFC62828);
  static const surfaceLight = Color(0xFFFFFFFF);
  static const backgroundLight = Color(0xFFF5F7FA);
  static const onSurfaceLight = Color(0xFF1A1A1A);
  static const onSurfaceVariantLight = Color(0xFF595959);
  static const outlineLight = Color(0xFFBDBDBD);

  static const surfaceDark = Color(0xFF1E1E1E);
  static const backgroundDark = Color(0xFF121212);
  static const onSurfaceDark = Color(0xFFEAEAEA);
  static const onSurfaceVariantDark = Color(0xFFA3A3A3);
  static const primaryDark = Color(0xFF8AB4F8);
  static const outlineDark = Color(0xFF3D3D3D);

  // Status not covered by the base semantic tokens (Part B.1 status mapping).
  static const statusBloque = Color(0xFFE65100);
  static const statusCloturee = Color(0xFF616161);
}
